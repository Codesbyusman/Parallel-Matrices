#include <iostream>

// Definition of the function
void processArray(int size, int* resultArray) {
    // Some processing to fill the array, for example
    for (int i = 0; i < size; ++i) {
        resultArray[i] = i * 2;
    }
}
